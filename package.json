{
  "name": "sfid",
  "version": "1.0.0",
  "description": "Utilities to handle Salesforce record IDs and API names",
  "main": "index.js",
  "scripts": {
    "test": "mocha --require ts-node/register --watch-extensions ts 'test/**/*.ts'",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "author": "Andrés Catalán (@catalandres)",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.5",
    "chai": "^4.2.0",
    "gts": "^0.8.0",
    "mocha": "^5.2.0",
    "ts-node": "^7.0.1",
    "typescript": "^2.8.4"
  }
}
